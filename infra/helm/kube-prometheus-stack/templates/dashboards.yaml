---
apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-dashboards
  labels:
    grafana_dashboard: "1"
data:
  grpc-metrics.json: |
    {{- .Files.Get "dashboards/grpc-metrics.json" | nindent 4 }}
